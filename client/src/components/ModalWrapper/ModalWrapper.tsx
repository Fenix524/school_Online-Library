import React from 'react';
	import './ModalWrapper.module.css';
	
	interface ModalWrapperProps {
		// Оголошення пропсів компонента
	}
	
	const ModalWrapper: React.FC<ModalWrapperProps> = (props) => {
		// Вміст компонента
	
		return (
			<div>
				{/* Вміст компонента */}
			</div>
		);
	};
	
	export default ModalWrapper;	
